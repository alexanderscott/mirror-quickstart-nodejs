"use strict";

var assert = require('assert'),
    authController = require('../app/controllers/AuthController');


describe('auth', function() {
    before(function(cb) {
        cb();
    });

    beforeEach(function(cb) {
        cb();
    });

    afterEach(function(cb) {
        cb();
    });

    after(function(cb) {
        cb();
    });

    describe('#getOauthCallback()', function() {
        it('', function(cb) {

            cb();
        });
    });
});
